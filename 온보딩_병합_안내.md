# 온보딩 + 홈페이지 병합 안내

온보딩(https://github.com/kimyebin726/onbording)을 이 레포에 합쳐, 사이트 진입 시 온보딩이 먼저 나오고
마지막 화면의 "계속하기" 버튼을 누르면 홈페이지로 넘어가게 했습니다. 온보딩 레포 자체는 건드리지 않았습니다.

## 바뀐 파일

| 파일 | 내용 |
|---|---|
| `index.html` | 온보딩 (onbording 레포의 index.html + 아래 수정 1곳) |
| `home.html` | 기존 홈페이지 (이전 index.html을 이름만 바꿈, 내용 동일) |
| `support.js` | 온보딩 런타임 (onbording 레포 그대로) |
| `assets/` | 온보딩 이미지 9장 + intro-bg.mp4 추가 (기존 파일과 이름 충돌 없음) |

## index.html 수정 내용 (온보딩 마지막 화면)

원래는 5번 화면의 "다음" 버튼이 1번 화면으로 되돌아갔습니다. 이걸 홈페이지로 넘어가게 바꿨습니다.

- 버튼 문구: "다음 →" → "계속하기" (영어 "Get started" 등 10개 언어 기존 번역 사용)
- 동작: `home.html`로 이동하면서 아래 두 가지를 넘겨줍니다.
  - 언어: 온보딩에서 고른 언어를 홈페이지가 읽는 `localStorage["tk3_lang"]`에 저장 (KR→kr, EN→en …)
  - 역할: "선생님으로 시작하기" → `home.html#home`, "학생으로 시작하기" → `home.html#shome`

## 확인한 것

- 학생·한국어 경로: 온보딩 5화면 → `home.html#shome`, 학생용 탭, 한국어
- 선생님·영어 경로: 온보딩 5화면 → `home.html#home`, Teacher 탭, 영어
- 홈페이지 내부 이동(해시 라우팅)은 파일명이 바뀌어도 그대로 동작

## 알아두실 점

- 기존에 공유된 `https://kimyebin726.github.io/ToolKIT/#shorts` 같은 링크는 이제 온보딩으로 갑니다.
  홈페이지로 직접 가려면 `https://kimyebin726.github.io/ToolKIT/home.html#shorts` 입니다.
- 온보딩은 React를 unpkg에서, 폰트를 jsdelivr에서 받아옵니다 (온보딩 레포 원래 구조). 오프라인에선 온보딩이 뜨지 않습니다.
- 온보딩은 매번 나옵니다. "한 번 본 사람은 건너뛰기"는 넣지 않았습니다. 필요하면 index.html에 몇 줄 추가로 가능합니다.

## GitHub 웹에서 올리는 순서 (푸시 권한 없는 경우)

1. 레포에서 기존 `index.html` 열기 → 연필(Edit) → 파일명을 `home.html`로 바꿔 커밋
2. Add file → Upload files → `index.html`, `support.js`, `assets/` 안의 새 파일 10개 업로드 (폴더 구조 유지)
3. 2~3분 뒤 https://kimyebin726.github.io/ToolKIT/ 확인

---

# 2차 병합: 홈페이지 최신본(viperfish) 반영 + 언어 3개로 축소

다른 작업자가 만든 홈페이지 최신본(`~/Downloads/viperfish/index.html`, 오너 커밋 73c53e6 기준 + 54곳 수정)을
`home.html`로 반영했습니다. 온보딩(index.html, support.js)은 그대로 두었고, 온보딩 → 홈페이지 연결도 유지됩니다.

## viperfish 쪽 변경 내용 (그대로 반영)
- 헤더 여백·히어로 여백 조정, 노래 섹션 재설계(6곡: 팡팡송 KR/EN/TH, 오늘도 팡, 팡팡 빛이야 KR/EN, 새 썸네일 6장)
- 영상·쇼츠·와이드: 로컬 파일 우선 재생(`<video>`), 없으면 Drive/YouTube iframe 폴백
- 영어 쇼츠 10편 로컬 파일(`assets/shorts/sh-*-en.mov`), 영어 영상자료 8편 로컬 파일(`assets/scattered/`, `assets/woundup/`),
  셀프응원 KR/EN 로컬 파일(`assets/tense/`), 게임 와이드 썸네일 KR/EN(`assets/game/`)
- 영어 문구 추가·교정

## 우리가 바꾼 것
1. **언어 선택을 한국어·영어·태국어 3개로 축소** — 홈페이지 `vals.langs`, 온보딩 `langs` 두 목록만 줄였습니다.
   다른 언어의 번역 데이터는 코드에 남아 있어, 나중에 목록에 다시 넣으면 그대로 동작합니다.
2. **100MB 초과 영상 20개 재인코딩** — GitHub은 파일당 100MB를 넘으면 push 자체가 거부되고, Pages 사이트는 1GB 제한이 있습니다.
   viperfish의 영상 원본은 셀프응원 673MB/663MB, 영어 영상자료 8편 52~147MB, 영어 쇼츠 10편 22~41MB(합 3.4GB)여서
   경로와 파일명은 그대로 두고 H.264로 다시 인코딩했습니다(셀프응원 4Mbps, 영상자료 6Mbps, 쇼츠 4.5Mbps).
   원본 화질이 필요하면 `~/Downloads/viperfish/assets/`에 그대로 있습니다.
3. viperfish에 있지만 코드에서 참조하지 않는 파일은 올리지 않았습니다
   (`assets/shorts/`의 영어 제목 .mov 13개, `assets/song/` 폴더, 재인코딩 전 원본).

## 검증
- 온보딩 언어 메뉴 3개, 홈페이지 언어 메뉴 3개
- 홈페이지: 노래 6곡 썸네일, 게임 와이드 썸네일, 영어 쇼츠·영어 영상자료·셀프응원 로컬 재생
- 온보딩 → 홈페이지 전환(언어·역할 전달) 유지
